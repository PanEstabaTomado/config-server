server:
  port: 8089

spring:
  cloud:
    discovery:
      enabled: true

springdoc:
  servers:
    - url: http://localhost:8098/api/bibliotecaam/usuario
      description: Via del Api Gateway

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8096/eureka/