server:
  port: 8093

spring:
  cloud:
    discovery:
      enabled: true

springdoc:
  servers:
    - uri: http://localhost:8098/api/bibiliotecaam/compra
      description: Via del Api Gateway

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8096/eureka/